services:
  server:
    build: ./server
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env

  client:
    build:
      context: ./client
      args:
        VITE_API_URL: http://localhost:3000
    ports:
      - "8080:80"
    depends_on:
      - server